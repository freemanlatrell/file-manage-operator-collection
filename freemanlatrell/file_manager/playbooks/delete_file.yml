- name: Delete a File
  hosts: all
  gather_facts: false
  vars_files:
    - vars/my_vars.yml

  tasks:
    - name: Delete the file in the /u/ibmuser/files directory
      ansible.builtin.file:
        path: /u/ibmuser/files/{{ filename }}
        state: absent
